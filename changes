for file in *.c *.h Makefile MENU* PROC* NOTE* TEXT* AAA_README QUICKSTART piglogo.d
do
    rcsdiff $file >/tmp/$file.changes 2>&1
    if [ $? != 0 ] 
    then
    	echo "***********"  $file "***********" 
	cat /tmp/$file.changes
    fi
   rm /tmp/$file.changes
done
